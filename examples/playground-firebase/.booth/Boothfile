# syntax=codingbooth/boothfile:1
# playground-firebase — lean booth to exercise Firebase CLI + host credential seed.
#
# `setup firebase` resolves to .booth/setups/firebase--setup.sh first (PATH),
# which runs the product installer then installs the placeholder-aware startup.

setup firebase
