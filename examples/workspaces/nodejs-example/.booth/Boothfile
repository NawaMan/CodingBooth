# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --select nodejs+vscode-ext+kernel/python/notebook/claude-code

env APT_SNAPSHOT=20260906T000000Z

arg CLAUDE_CODE_VERSION=latest
arg NODE_VERSION=22
arg NOTEBOOK_PORT=18888
arg PYTHON_VERSION=3.13.12

setup claude-code ${CLAUDE_CODE_VERSION}
setup nodejs ${NODE_VERSION}
setup python ${PYTHON_VERSION}
setup notebook ${NOTEBOOK_PORT}
setup nodejs-code-extension
setup python-code-extension
setup nodejs-nb-kernel
