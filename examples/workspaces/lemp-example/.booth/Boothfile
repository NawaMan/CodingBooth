# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --expose +-1920:80 --select php~vscode-ext/mysql/nginx+php-fpm/lemp-init

env APT_SNAPSHOT=20260906T000000Z

arg PHP_VERSION=8.3

setup lemp-init
setup mysql
setup nginx
setup php --version ${PHP_VERSION}
setup nginx --fpm-only
