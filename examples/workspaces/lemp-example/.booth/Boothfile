# syntax=codingbooth/boothfile:1
# Hand-written: `setup nginx --with-php-fpm` plus local lemp-init; the nginx template tells you to hand-edit for php-fpm. Do not add a "# Configured by:" header.
# LEMP example — Linux + nginx + MySQL + PHP-FPM, single container

setup php
setup mysql
setup nginx --with-php-fpm
setup lemp-init
