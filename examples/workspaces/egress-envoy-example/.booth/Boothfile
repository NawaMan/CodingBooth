# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --set egress --set egress-policy-file=.booth/egress/envoy.yaml --select claude-code~credential

env APT_SNAPSHOT=20260906T000000Z

arg CLAUDE_CODE_VERSION=latest

setup claude-code ${CLAUDE_CODE_VERSION}
