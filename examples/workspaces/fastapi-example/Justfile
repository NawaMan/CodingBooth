run := if env("BOOTH_CONTAINER_NAME", "") == "" { "./booth exec --run --" } else { "" }

[private]
default:
    @just --list

# Install deps and serve on :8000 (Swagger UI at /docs)
run:
    {{run}} bash -c 'pip install -r requirements.txt && uvicorn app:app --host 0.0.0.0 --port 8000 --reload'
