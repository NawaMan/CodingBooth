# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --select apt-pkg:jq,tree,ripgrep

env APT_SNAPSHOT=20260916T000000Z

arg APT_PKGS=jq,ripgrep,tree

install apt ${APT_PKGS}
