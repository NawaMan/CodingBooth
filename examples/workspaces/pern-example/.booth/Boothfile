# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --expose +-7000:3000 --expose +-4827:5173 --select nodejs/postgresql

env APT_SNAPSHOT=20260906T000000Z

arg NODE_VERSION=22
arg PG_VERSION=latest

setup nodejs ${NODE_VERSION}
setup postgresql --version ${PG_VERSION}
setup nodejs-code-extension
