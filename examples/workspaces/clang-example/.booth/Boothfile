# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --select clang/cmake/make/apt-pkg:nlohmann-json3-dev

env APT_SNAPSHOT=20260918T000000Z

arg APT_PKGS=nlohmann-json3-dev
arg CMAKE_VERSION=latest
arg LLVM_VERSION=18
arg MAKE_VERSION=apt

install apt ${APT_PKGS}
setup clang --version ${LLVM_VERSION}
setup cmake --version ${CMAKE_VERSION}
setup make --version ${MAKE_VERSION}
setup clang-code-extension
