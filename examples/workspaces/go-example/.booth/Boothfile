# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --variant xfce --select go+vscode-ext+kernel/python/notebook/codeserver

env APT_SNAPSHOT=20260906T000000Z

arg CODESERVER_PORT=19999
arg GO_VERSION=1.25.7
arg NOTEBOOK_PORT=18888
arg PYTHON_VERSION=3.13.12

setup go ${GO_VERSION}                                # GO tool chain
install go golang.org/x/tools/gopls@latest            # GO language server
install go github.com/go-delve/delve/cmd/dlv@latest   # Debugger
setup python ${PYTHON_VERSION}
setup codeserver ${CODESERVER_PORT}
setup notebook ${NOTEBOOK_PORT}
setup go-code-extension
setup python-code-extension
setup go-nb-kernel
