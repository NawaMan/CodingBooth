# syntax=codingbooth/boothfile:1
# Hand-written: local mean-init setup and a commented-out run-args publish; neither has a catalog home. Do not add a "# Configured by:" header.
# MEAN example — MongoDB + Express + Angular + Node

arg NODE_VERSION=22

setup nodejs ${NODE_VERSION}
setup mongodb
setup mean-init
