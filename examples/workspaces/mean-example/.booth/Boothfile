# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --expose +-5800:4200 --select nodejs~vscode-ext/mongodb+start

env APT_SNAPSHOT=20260906T000000Z

arg MONGO_VERSION=latest
arg NODE_VERSION=22

setup mongodb --version ${MONGO_VERSION}
setup nodejs ${NODE_VERSION}
setup mongodb-start
