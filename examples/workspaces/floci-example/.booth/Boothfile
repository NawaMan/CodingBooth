# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --select aws-cli/floci+autostart+expose

env APT_SNAPSHOT=20260906T000000Z

arg AWS_CLI_VERSION=latest
arg FLOCI_HOST_PORT=4566
arg FLOCI_PORT=4566
arg FLOCI_VERSION=latest

setup aws-cli --version ${AWS_CLI_VERSION}
setup dind
setup floci --version ${FLOCI_VERSION}
