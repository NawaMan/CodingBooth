# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --select java+vscode-ext/lazygit

env APT_SNAPSHOT=20260905T000000Z

arg JDK_VENDOR=temurin
arg JDK_VERSION=25
arg LAZYGIT_VERSION=latest

setup jdk ${JDK_VERSION} ${JDK_VENDOR}
setup lazygit --version ${LAZYGIT_VERSION}
setup java-code-extension
