# syntax=codingbooth/boothfile:1
# Hand-written: `setup php --with-composer` and `setup apache --with-php` plus local wp-init — catalog templates do not emit those flags. Do not add a "# Configured by:" header.
# WordPress example — PHP + Apache (mod_php) + MySQL on one container

setup php --with-composer
setup mysql
setup apache --with-php
setup wp-init
