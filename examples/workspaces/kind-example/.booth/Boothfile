# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --select dind/kind/xfce/claude-code/k9s
# Hand-edited afterward: antigravity dropped, k9s + remove-firefox + chrome-bookmarks
# added (the latter two are project-local, see .booth/setups/).

env APT_SNAPSHOT=20260906T000000Z

arg CLAUDE_CODE_VERSION=latest
arg KIND_VERSION=0.27.0
arg K9S_VERSION=latest

setup xfce
setup claude-code ${CLAUDE_CODE_VERSION}
setup dind
setup kind --kind-version ${KIND_VERSION}
setup k9s --version ${K9S_VERSION}
setup remove-firefox
setup chrome-bookmarks
