# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --expose +-1920:8080 --select java:21+maven:3.9.11+vscode-ext/nodejs

env APT_SNAPSHOT=20260906T000000Z

arg JDK_VENDOR=temurin
arg JDK_VERSION=21
arg MAVEN_VERSION=3.9.11
arg NODE_VERSION=22

setup jdk ${JDK_VERSION} ${JDK_VENDOR}
setup mvn ${MAVEN_VERSION}
setup nodejs ${NODE_VERSION}
setup java-code-extension
setup nodejs-code-extension
