# syntax=codingbooth/boothfile:1
# Hand-written: live writable ~/.config/gcloud bind (gcloud SQLite WAL); +credential seeds read-only files and does not work here. Do not add a "# Configured by:" header.

# Google Cloud Example
# Installs Google Cloud CLI on top of the base workspace image

setup gcloud

setup claude-code
setup antigravity
