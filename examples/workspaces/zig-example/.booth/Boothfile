# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --select zig

env APT_SNAPSHOT=20260807T000000Z

arg ZIG_VERSION=0.15.1

setup zig --zig-version ${ZIG_VERSION}
setup zig-code-extension
