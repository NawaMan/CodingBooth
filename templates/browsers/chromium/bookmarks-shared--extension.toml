display-name = "Bookmarks Shared"
display-disc = "Team-share Chromium profile Default/ via git (.booth/shared)"
display-detail = """Live-bind-mounts ~/.chrome-data/Default/ under .booth/shared/ so Chromium can rename Bookmarks in place (a single-file mount of Bookmarks breaks). Prefer committing only Bookmarks via .gitignore inside the shared Default dir — cookies/logins may appear under Default; do not commit secrets. Full local profiles: +profile-cache or --persist-home."""
display-order = 92
tags = ["persistence", "shared", "browser", "bookmarks", "git"]

shared-dirs = [
    "home/coder/.chrome-data/Default",
]
