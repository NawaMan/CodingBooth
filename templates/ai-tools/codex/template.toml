display-name = "Codex"
primary = true
display-disc = "OpenAI Codex CLI for AI-assisted coding"
display-detail = "Installs OpenAI Codex CLI for AI-assisted coding from the terminal. Requires Node.js which is auto-installed."
display-order = 12
tags = [
    "ai",
    "openai"
]
requires = ["nodejs"]

[params.CODEX_VERSION]
default = "latest"
suggests = ["latest"]

[segments]
"Boothfile--60" = """
setup codex ${CODEX_VERSION}
"""
