# NOTE: This template has not been tested -- no time (sorry). Please report success or failure. :-p
display-name = "Codex Credentials"
display-disc = "Mount host Codex config and credentials into the booth"
display-order = 90
auto-select = true
tags = ["credential", "openai"]

run-args = [
    # OpenAI Codex config and credentials (home-seeding: may update tokens/session)
    "-v", "~/.codex:/etc/cb-home-seed/.codex:ro",
]
