# NOTE: This template has not been tested -- no time (sorry). Please report success or failure. :-p
display-name = "Claude Code Credentials"
display-disc = "Mount host Claude config and credentials into the booth"
display-order = 90
auto-select = true
tags = ["credential", "claude"]

run-args = [
    # Claude Code config and credentials (home-seeding: may update tokens/session)
    "-v", "~/.claude.json:/etc/cb-home-seed/.claude.json:ro",
    "-v", "~/.claude:/etc/cb-home-seed/.claude:ro",
]
