# NOTE: This template has not been tested -- no time (sorry). Please report success or failure. :-p
display-name = "Warp Credentials"
display-disc = "Mount ~/.warp credentials from host (read-only home-seeding)"
auto-select = true
tags = ["warp", "credential"]

run-args = [
    "-v", "~/.warp:/etc/cb-home-seed/.warp:ro"
]
