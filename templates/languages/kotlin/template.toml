display-name = "Kotlin"
display-disc = "Kotlin language for JVM development (requires java)"
display-detail = "Installs Kotlin for JVM development. Requires the java template for JDK. Supports version pinning via the KOTLIN_VERSION parameter."
display-order = 32
tags = [
    "kotlin",
    "jvm"
]
requires = ["java"]

[params.KOTLIN_VERSION]
default = "latest"
suggests = ["latest", "2.0.20", "2.0.0", "1.9.24"]

[segments]
"Boothfile--60" = """
setup kotlin --version ${KOTLIN_VERSION}
"""
