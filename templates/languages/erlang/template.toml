display-name = "Erlang"
display-disc = "Erlang/OTP runtime for concurrent applications"
display-detail = "Installs the Erlang/OTP platform for building concurrent, fault-tolerant distributed systems."
display-order = 72
tags = [
    "erlang",
    "functional",
    "distributed"
]

[params.OTP_VERSION]
default = "27"
suggests = ["27", "26", "25"]

[segments]
Boothfile = """
setup erlang --otp-version ${OTP_VERSION}
"""
