display-name = "Bun"
display-disc = "Bun fast JavaScript runtime and bundler"
display-detail = "Installs Bun, a fast all-in-one JavaScript runtime with built-in bundler, transpiler, and package manager."
display-order = 52
tags = [
    "bun",
    "javascript",
    "typescript"
]

[params.BUN_VERSION]
default = "latest"
suggests = ["latest"]

[segments]
Boothfile = """
setup bun ${BUN_VERSION}
"""
