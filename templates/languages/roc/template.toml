display-name = "Roc"
display-disc = "Roc functional programming language"
display-detail = "Installs the Roc compiler (fast, friendly, functional) from upstream prebuilt binaries. Linked at /opt/roc-stable."
display-order = 78
tags = [
    "roc",
    "functional",
    "compiled",
]

[params.ROC_VERSION]
default = "alpha4-rolling"
suggests = ["alpha4-rolling", "alpha3-rolling", "nightly"]

[segments]
Boothfile = """
setup roc --version ${ROC_VERSION}
"""
