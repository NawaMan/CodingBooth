display-name = "Node.js"
primary = true
display-disc = "Node.js runtime with npm package manager"
display-detail = "Installs Node.js with npm for JavaScript and TypeScript development. Supports version pinning via the NODEJS_VERSION parameter."
display-order = 50
tags = [
    "nodejs",
    "javascript",
    "backend"
]

[params.NODE_VERSION]
default = "22"
suggests = ["22", "20", "18"]

[segments]
Boothfile = """
setup nodejs ${NODE_VERSION}
"""
