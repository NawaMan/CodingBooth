display-name = "SSH Credentials"
display-disc = "Mount host SSH keys and config into the booth"
display-order = 3
auto-select = false
tags = [
    "ssh",
    "credential",
    "git"
]

run-args = [
    "-v", "~/.ssh:/etc/cb-home-seed/.ssh:ro",
]
