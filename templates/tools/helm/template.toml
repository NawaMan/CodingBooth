display-name = "Helm"
display-disc = "Kubernetes package manager"
display-detail = "Installs Helm for managing Kubernetes applications via charts, repositories, and releases."
display-order = 57
tags = ["kubernetes", "k8s", "helm", "cloud", "cli"]

[params.HELM_VERSION]
default = "latest"
suggests = ["latest"]

[segments]
Boothfile = """
setup helm --version ${HELM_VERSION}
"""
