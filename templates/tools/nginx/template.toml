display-name = "nginx"
display-disc = "High-performance reverse proxy and web server"
display-detail = "Installs nginx. The daemon auto-starts on container boot. Add the php-fpm extension (`nginx+php-fpm`) to install php-fpm and route .php files through it on the default site."
display-order = 39
tags = [
    "web",
    "server",
    "http",
    "proxy",
    "lemp",
]

[segments]
Boothfile = """
setup nginx
"""
