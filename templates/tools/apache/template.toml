display-name = "Apache HTTP Server"
display-disc = "Battle-tested web server (apache2)"
display-detail = "Installs the Apache HTTP Server (apache2). The daemon auto-starts on container boot. Add the php extension (`apache+php`) to enable mod_php so .php files are served from /var/www/html."
display-order = 38
tags = [
    "web",
    "server",
    "http",
    "lamp",
]

[segments]
Boothfile = """
setup apache
"""
