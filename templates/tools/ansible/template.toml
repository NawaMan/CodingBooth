display-name = "Ansible"
display-disc = "Ansible automation and configuration management tool"
display-detail = "Installs Ansible for IT automation, configuration management, and application deployment via playbooks."
display-order = 65
tags = [
    "ansible",
    "devops",
    "automation",
    "cli"
]

[params.ANSIBLE_VERSION]
default = "latest"
suggests = ["latest"]

[segments]
Boothfile = """
setup ansible --version ${ANSIBLE_VERSION}
"""
