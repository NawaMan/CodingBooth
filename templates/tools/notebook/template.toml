display-name = "Jupyter Notebook"
display-disc = "JupyterLab notebook server (requires python)"
display-detail = "Sets up JupyterLab with a Bash kernel for interactive notebook development. Requires the python template. Expose on a configurable port."
display-order = 24
requires = ["python"]
tags = [
    "jupyter",
    "notebook",
    "python"
]

[segments]
"Boothfile--60" = """
setup notebook 18888
"""
