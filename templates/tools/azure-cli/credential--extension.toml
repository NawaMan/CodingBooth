display-name = "Azure Credentials"
display-disc = "Mount host Azure CLI credentials"
auto-select = true
tags = ["azure", "credential"]

run-args = [
    "-v", "~/.azure:/etc/cb-home-seed/.azure:ro"
]
