# NOTE: This template has not been tested -- no time (sorry). Please report success or failure. :-p
display-name = "Docker Config"
display-disc = "Mount host Docker configuration files"
auto-select = true
tags = ["docker", "credential"]

run-args = [
    "-v", "~/.docker/config.json:/etc/cb-home-seed/.docker/config.json:ro"
]
