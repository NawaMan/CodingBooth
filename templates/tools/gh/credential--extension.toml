# NOTE: This template has not been tested -- no time (sorry). Please report success or failure. :-p
display-name = "GitHub CLI Credentials"
display-disc = "Mount ~/.config/gh credentials from host (read-only home-seeding)"
auto-select = true
tags = ["github", "credential"]

run-args = [
    "-v", "~/.config/gh:/etc/cb-home-seed/.config/gh:ro"
]
