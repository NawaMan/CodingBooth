display-name = "Settings Shared"
display-disc = "Team-share Chromium settings via git (.booth/shared) — not cache"
display-detail = """Live-bind-mounts ~/.chrome-data/Default/ under .booth/shared/ (same path as Google Chrome wrapper). Directory mount for Preferences renames. shared only — not .booth/cache. Secrets: omit cookies/logins from git."""
display-order = 93
tags = ["persistence", "shared", "browser", "settings", "git"]

shared-dirs = [
    "home/coder/.chrome-data/Default",
]
