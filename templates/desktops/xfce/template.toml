display-name = "XFCE"
display-disc = "XFCE lightweight desktop with VNC/noVNC access"
display-detail = "Provides a full XFCE desktop environment accessible via VNC and noVNC in the browser. Supports custom screen resolution via the GEOMETRY environment variable."
display-order = 14
tags = [
    "desktop",
    "xfce",
    "lightweight",
    "vnc"
]

[segments]
"Boothfile--40" = """
setup xfce
"""
