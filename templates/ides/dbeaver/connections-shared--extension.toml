display-name = "Connections Shared"
display-disc = "Team-share DBeaver data-sources.json via git (.booth/shared)"
display-detail = "Live-bind-mounts DBeaver's data-sources.json under .booth/shared/ so connection definitions (host, port, database, user) travel with the repo. Never commit passwords or secrets — leave password fields empty and seed credentials via home-seed/host mounts instead. Path matches the DBeaver CE workspace layout used by CodingBooth examples."
display-order = 92
tags = ["persistence", "shared", "database", "dbeaver", "git"]

shared-files = [
    "home/coder/.local/share/DBeaverData/workspace6/General/.dbeaver/data-sources.json",
]
