display-name = "GoLand Open Project"
display-disc = "Open the workspace project automatically, no Open-project wizard"
display-detail = "Like IntelliJ, GoLand needs no separate workspace registration -- a folder with a go.mod (or any Go sources) opens directly, no import wizard. GoLand's \"recent projects\" list lives in internal app state, not an editable file, so it can't be safely pre-seeded -- instead this patches the IDE's own starter script to default to opening the project directory whenever launched with no arguments, which both the CLI shim and the desktop icon already funnel through."
display-order = 1
auto-select = true
tags = [
    "jetbrains", "goland", "ide", "go", "open"
]

[segments]
"Boothfile--65" = """
setup jetbrains-import-project goland
"""
