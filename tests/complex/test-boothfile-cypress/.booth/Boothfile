# syntax=codingbooth/boothfile:1
# Phase 3: Cypress + shared binary cache
# cypress--setup.sh is under .booth/setups/ until it ships in the Hub base image.

setup nodejs 22
setup cypress --version latest
