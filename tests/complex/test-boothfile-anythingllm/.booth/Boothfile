# syntax=codingbooth/boothfile:1

# The released base image does not yet ship anythingllm--setup.sh; the copy
# under .booth/setups is the one under test. Keep it byte-identical with
# variants/base/setups/anythingllm--setup.sh.
#
# The fixture is a tiny Node server that answers /api/ping the same way the
# real AnythingLLM health check does. Pulling mintplexlabs/anythingllm here
# would add several GB to a unit-of-wiring test.

copy ./fixtures/anythingllm /opt/anythingllm
setup anythingllm --port 3001
