# syntax=codingbooth/boothfile:1
# Phase 3: Selenium drivers + Chrome for Testing
# selenium--setup.sh is under .booth/setups/ until it ships in the Hub base image.

setup selenium chromium --chrome-version Stable
