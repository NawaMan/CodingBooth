# syntax=codingbooth/boothfile:1

# xfce--setup.sh is loaded from .booth/setups/ (mirrors
# variants/base/setups/xfce--setup.sh) until the released base image ships
# the Fira Code Nerd Font install.

setup xfce
