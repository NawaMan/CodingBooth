# syntax=codingbooth/boothfile:1
# Phase 3: Puppeteer + shared Chromium cache
# puppeteer--setup.sh is under .booth/setups/ until it ships in the Hub base image.

setup nodejs 22
setup puppeteer --version latest
