# syntax=codingbooth/boothfile:1

# Test: Mojo Jupyter kernel (%%mojo magic on a Mojo-named Python kernel).
# mojo--setup.sh and mojo-nb-kernel--setup.sh are under .booth/setups/ until
# the base image ships them. python/notebook setups are already in the image.

setup python 3.13.12
setup notebook
setup mojo --version 1.0.0
setup mojo-nb-kernel
