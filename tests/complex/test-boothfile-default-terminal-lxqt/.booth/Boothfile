# syntax=codingbooth/boothfile:1

# Test: Alacritty made the default terminal on LXQt
# alacritty--setup.sh and default-terminal--setup.sh are not yet in the
# released base image, so copies are checked in under .booth/setups/ to
# override them for this test run.
setup alacritty
setup default-terminal alacritty
