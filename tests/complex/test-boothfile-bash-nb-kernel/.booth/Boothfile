# syntax=codingbooth/boothfile:1

# Test: Bash Jupyter kernel installation via Boothfile.
# Matches what `booth config --select bash-nb-kernel` would emit:
# bash-nb-kernel requires notebook, which requires python.
setup python
setup notebook
setup bash-nb-kernel
