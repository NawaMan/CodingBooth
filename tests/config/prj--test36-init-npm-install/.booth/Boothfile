# syntax=codingbooth/boothfile:1
# Configured by: booth init adjust --select nodejs+npm-install

arg NODE_VERSION=22

setup nodejs ${NODE_VERSION}
setup nodejs-code-extension
run --mount=type=bind,target=/tmp/ctx if [ -f /tmp/ctx/package.json ]; then mkdir -p /opt/npm-cache && cp /tmp/ctx/package.json /opt/npm-cache/ && cp /tmp/ctx/package-lock.json /opt/npm-cache/ 2>/dev/null || true && cd /opt/npm-cache && npm install; fi
