# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --select deno+pkg:npm:cowsay

arg DENO_PKGS=npm:cowsay
arg DENO_VERSION=latest

setup deno ${DENO_VERSION}
install deno-pkg ${DENO_PKGS}
setup deno-code-extension
