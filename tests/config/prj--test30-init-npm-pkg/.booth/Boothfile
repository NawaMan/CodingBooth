# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --select nodejs+npm-pkg:cowsay

arg NODE_VERSION=22
arg NPM_PKGS=cowsay

setup nodejs ${NODE_VERSION}
install npm ${NPM_PKGS}
setup nodejs-code-extension
