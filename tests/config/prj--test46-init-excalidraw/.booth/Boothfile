# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --select excalidraw:889+expose+autostart

arg EXCALIDRAW_PORT=889
arg NODE_VERSION=22

setup nodejs ${NODE_VERSION}
setup excalidraw ${EXCALIDRAW_PORT}
setup nodejs-code-extension
