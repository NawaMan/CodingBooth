# Source repository

repo: NawaMan/CodingBooth
branch: main
path: templates

## Last sync

date: 2026-09-06T04:53:03Z

### Updated in this project

- Exported `--select` string now follows the real grammar from `docs/AGENT_RECIPE.md`: templates joined by `/`, params comma-separated positionally after one `:`, extensions with `+`, auto-select opt-outs with `~`, an extension's pinned param as `+expose:19000`.
- Generated-file header uses the repo's own two-line `# Generated by:` / `# Adjust with :` re-generation form, and the shareable tab also emits the selection as a `.booth/recipes/*.recipe` body.

- Catalog rebuilt from the real `templates/` tree: 153 templates across the repo's eight groups, with their real extension lists (207 `*--extension.toml` files).
- Real params, defaults and `suggests` for the templates read directly (go, python, nodejs, java, rust, kotlin, postgresql, redis, kafka, codeserver, code-ext-pkg, jetbrains-plugin-pkg, playwright, cloudbeaver, notebook, dbeaver, idea, apt-pkg, brew-pkg, gh, dind, ollama, claude-code, chromium, google-chrome, xfce, thonny, nbgrader).
- `google-chrome` carries its real `unsupported-arch = ["arm64"]` note and named substitute; `kotlin` → `java`, `notebook` → `python`, `playwright` → `nodejs`, `brew-pkg` → `homebrew` are the repo's own `requires` edges.
- Expose/autostart/vscode-ext/kernel/credential/`*-pkg` extension behaviour follows `templates/README.md` (host port follows the service port; `+OFFSET` relative to offset base).

## Screen map

| Project screen | Built from |
| --- | --- |
| `Config Web UI.dc.html` — catalog, package rail, preview & save | `templates/README.md` (segment order, server autostart/expose convention, two-ports rule, code-ext-pkg rationale) |
| `catalog.js` — template/extension data | `templates/*/*/template.toml`, `templates/*/*/*--extension.toml`, `templates/*/meta.toml` |
| `Config Web UI.dc.html` — shareable DSL / recipe export, generated headers | `docs/AGENT_RECIPE.md` |

## Sync history

- 2026-09-06T04:45:00Z — initial import: catalog rebuilt from `templates/` (153 templates, 207 extensions), real params/`suggests`/`requires`, `google-chrome` arm64 note.
